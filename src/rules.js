export default {
    /**
     * 
     * @param {element value} value 
     * @param {validation rule option} rules 
     * @param {element id} id 
     * @param {element all index values if element is array} values 
     * @param {validation options} options 
     */
    myTestValidation: function (value, rules,  id, values, options) {
        // return fileValidation(value, rules, id, formName, formValues)
        // console.log('Test value ', value)
        // console.log('Test rules ', rules)
        // console.log('Test id ', id)
        // console.log('Test values ', values)
        // console.log('Test options ', options)
        // rules, id, formName, formValues)
        return 'I am test error'
      }
} 